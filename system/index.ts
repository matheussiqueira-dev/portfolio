/**
 * @/system
 * Business logic layer — feature-specific components and logic
 * 
 * Usage:
 *   import { ProjectCard, ProjectList } from '@/system/projects'
 *   (Future) import { SystemDashboard } from '@/system/dashboard'
 */

// Projects system
export * from './projects';

// Note: Other systems are accessed from their own subfolders:
// @/system/projects
// (Future) @/system/dashboard
