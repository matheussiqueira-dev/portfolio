# Tests

This directory is reserved for automated tests.

Current status:
- Legacy tests that referenced removed modules were retired during repository cleanup.
- Add new tests here by domain (example: `tests/system/*.test.ts`).
